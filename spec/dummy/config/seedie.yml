default_count: 5
models:
  user:
    attributes:
      name: "name {{index}}"
      email: "email {{index}}"
    disabled_fields: []
  post: &post
    attributes:
      title: "title {{index}}"
    associations:
      has_many:
        comments: 4
    disabled_fields: []
  comment:
    attributes:
