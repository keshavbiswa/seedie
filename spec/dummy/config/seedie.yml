default_count: 5
models:
  user:
    attributes:
      name: "name {{index}}"
      email: "email {{index}}"
    disabled_fields: []
  post:
    attributes:
      title: "title {{index}}"
    associations:
      comments: 4
    disabled_fields: []
  comment:
    attributes:
      user_id: 

